#ifndef VAR15_H_INCLUDED
#define VAR15_H_INCLUDED


#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))

void prnt(int arr[], int len);

void generate(int arr[], int a, int b, int len);

main15();


#endif // VAR15_H_INCLUDED
